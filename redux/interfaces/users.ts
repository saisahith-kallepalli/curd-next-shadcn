// Define an interface for signup data
export interface Signup {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  // Add other properties for signup as needed
}

// Define an interface for another type of data (e.g., users)
export interface User {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  // Add other properties for user as needed
}
